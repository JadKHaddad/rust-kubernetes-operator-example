apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: echoes.example.com
spec:
  group: example.com
  names:
    categories: []
    kind: Echo
    plural: echoes
    shortNames: []
    singular: echo
  scope: Namespaced
  versions:
    - additionalPrinterColumns: []
      name: v1
      schema:
        openAPIV3Schema:
          description: Auto-generated derived type for EchoSpec via `CustomResource`
          properties:
            spec:
              description: Struct corresponding to the Specification (`spec`) part of the `Echo` resource, directly reflects context of the `echoes.example.com.yaml` file to be found in this repository. The `Echo` struct will be generated by the `CustomResource` derive macro.
              properties:
                replicas:
                  format: int32
                  type: integer
              required:
                - replicas
              type: object
            status:
              enum:
                - ok
                - error
              nullable: true
              type: string
          required:
            - spec
          title: Echo
          type: object
      served: true
      storage: true
      subresources:
        status: {}
